import Link from 'next/link';import {courses} from '@/data/courses';export const metadata={title:'Cursos Técnicos | IBESC',description:'Cursos técnicos do IBESC em Boa Viagem — CE.'};export default function Page(){const list=courses.filter(c=>c.category==='TECNICO');return <main><section className="hero"><div className="container"><span className="eyebrow" style={{background:'rgba(255,255,255,.12)',color:'#fff'}}>Cursos técnicos</span><h1>Formação prática para novas oportunidades.</h1><p>Prepare-se para o mercado de trabalho com cursos técnicos do IBESC.</p></div></section><section className="section"><div className="container"><div className="course-grid">{list.map(c=><article className="card course-card" key={c.id}><div className="course-image"/><span className="tag">Curso Técnico</span><h3>{c.name}</h3><div className="course-meta">IBESC • {c.area}</div><p>{c.description}</p><Link className="btn btn-dark" href={`/curso/${c.slug}`}>Quero saber mais</Link></article>)}</div></div></section></main>}
