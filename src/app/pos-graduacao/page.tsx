import Link from 'next/link';import {courses} from '@/data/courses';export const metadata={title:'Pós-graduação | IBESC',description:'Encontre sua pós-graduação em Boa Viagem — CE.'};export default function Page(){const list=courses.filter(c=>c.category==='POS_GRADUACAO');return <main><section className="hero"><div className="container"><span className="eyebrow" style={{background:'rgba(255,255,255,.12)',color:'#fff'}}>Pós-graduação</span><h1>Sua graduação abriu uma porta. Sua especialização pode abrir muitas outras.</h1><p>Encontre uma pós-graduação para aprofundar seus conhecimentos e avançar profissionalmente.</p></div></section><section className="section"><div className="container"><div className="course-grid">{list.map(c=><article className="card course-card" key={c.id}><div className="course-image"/><span className="tag">Especialização</span><h3>{c.name}</h3><div className="course-meta">UNINASSAU / UNIFAEL • {c.area}</div><p>{c.description}</p><Link className="btn btn-dark" href={`/curso/${c.slug}`}>Saiba mais</Link></article>)}</div></div></section></main>}
