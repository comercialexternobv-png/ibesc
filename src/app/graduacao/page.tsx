import Link from 'next/link';import {courses} from '@/data/courses';export const metadata={title:'Graduação | IBESC',description:'Encontre sua graduação em Boa Viagem — CE.'};export default function Page(){const list=courses.filter(c=>c.category==='GRADUACAO');return <main><section className="hero"><div className="container"><span className="eyebrow" style={{background:'rgba(255,255,255,.12)',color:'#fff'}}>Graduação</span><h1>Encontre sua graduação</h1><p>Escolha uma formação que acompanhe seus objetivos profissionais.</p></div></section><section className="section"><div className="container"><div className="course-grid">{list.map(c=><article className="card course-card" key={c.id}><div className="course-image"/><span className="tag">{c.type}</span><h3>{c.name}</h3><div className="course-meta">{c.institution} • {c.area}</div><p>{c.description}</p><Link className="btn btn-dark" href={`/curso/${c.slug}`}>Saiba mais</Link></article>)}</div></div></section></main>}
